def main() {
  var first<2, 3> = [1, 2, 3, 4, 5, 6];
  var second<2, 3> = [8, 2, 1, 2, 4, 2];

  # var res = mat_add(first, second);

  var res2 = mat_add(transpose(first), transpose(second));

  #print(res);
  print(res2);
}

